<main class="form form-shopping-list">
  <%= simple_form_for(shopping_list) do |f| %>
    <div class="h1 mb-3 fw-normal">
      <i class="bi <%= header_icon %>"></i>
      <%= header_text %>
    </div>

    <% if f.object.errors[:base].present? %>
      <%= render partial: "shared/flash_message",
                  locals: {
                    flash_type: "alert",
                    flash_message: f.object.errors[:base].to_sentence
                  }
      %>
    <% end %>
    <div data-controller="date-picker" class="form-inputs">
      <%= f.input :shopping_list_name,
                  required: true,
                  label: "Name",
                  autofocus: true
      %>
      <%= f.input :week_of, as: :string,
                  required: true,
                  input_html: {
                    autocomplete: "off",
                    class: 'datepicker form-control'
                  }
      %>
      <%= f.input :notes %>
      <%= f.input :template %>
    </div>

    <div class="form-actions">
      <%= f.button :submit,
                    submit_button_text,
                    class: "btn btn-primary"
      %>
    </div>
  <% end %>

  <div class="help-links">
    <%= link_to "Back", back_path %>
  </div>
</main>
